version: "2.0"

actions:
  - action_main_menu

intents:
  - menu

nlu:
  - intent: menu
    examples: |
      - മെനു

rules:
  - rule: Command menu
    steps:
      - intent: menu
      - action: action_deactivate_loop
      - action: action_main_menu
